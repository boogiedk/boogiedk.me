export interface CreateMeetModel {
    title: string;
    description: string;
    mapsLink: string;
    agreement: boolean;
}

export interface MeetModel {
    meetId: string;
    title: string;
    description: string;
    mapsLink: string;
    agreement: boolean;
}